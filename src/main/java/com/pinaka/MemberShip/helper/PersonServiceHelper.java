package com.pinaka.MemberShip.helper;

import com.pinaka.MemberShip.collection.Person;
import com.pinaka.MemberShip.dto.response.PersonResponse;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

public class PersonServiceHelper {

/*
    public List<PersonResponse> generateResponse(List<Person> personList){
        List<PersonResponse> response=new ArrayList<>();

        for(Person person :personList){
            PersonResponse personResponse=new PersonResponse();
            personResponse.setPersonId(person.getPersonId());
            personResponse.setAge(person.getAge());
            personResponse.setGender(person.getGender());
            personResponse.setDiscount(person.getDiscount());
            personResponse.setContactNo(person.getContactNo());
            personResponse.setFirstName(person.getFirstName());
            personResponse.setLastName(person.getLastName());
            personResponse.setStartDate(person.getStartDate());
            personResponse.setEndDate(person.getEndDate());
            personResponse.setMembership(person.getMembership());
            personResponse.setTotal(person.getTotal());

            response.add(personResponse);
        }
        return response;

    }*/



}
